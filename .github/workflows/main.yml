name: Deploy

on: [push]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' # Adjust to your branch
    steps:
      - name: Start Deployment
        run: echo "🚀 Starting deployment process..."

      - uses: actions/checkout@v3

      - name: Deploy to production via FTP
        uses: SamKirkland/FTP-Deploy-Action@4.4.0
        with:
          server:  51.77.72.43
          username: cicd@v1.sendtruly.com
          password: 1mZs88j@YI(h5Y
          protocol: ftps # Change to `ftp` if you want plain FTP
          local-dir: ./ # Directory to upload (default: repository root)
          server-dir: /home/sendtrul/v1.sendtruly.com # Destination directory on server
